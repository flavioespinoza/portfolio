"use strict";

console.log('fetch form .... hello!')

const el = document.getElementById('root');

// make a small application (very small) that would accept text from the form 
// and send it to the server, receiving a response from the server, filter it a 
// little and display it on the page